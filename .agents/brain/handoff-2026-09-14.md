# Handoff: Textbook-Driven Development campaign, paused for owner review

Date: 2026-09-14. Written by the orchestrating agent at the owner's request.
Agent work is stopped until the owner resumes it. Nothing in this file is
execution authority; it records state, open items, and how to resume.

## State at handoff

- Branch `dev/curriculum` at `b5022fe`, pushed to `origin/dev/curriculum`,
  141 commits ahead of `main`. Working tree clean.
- `main` is untouched at the pre-campaign v0.1.0 (`5ca071c`). The docs site
  deploys from `main` only, so nothing new is published.
- `dev/Fusai` is untouched at `635767b`, local only, never pushed. Its history
  contains `labs/`; do not push or rewrite it.
- `labs/` (private textbook workbooks) is gitignored, present in the working
  tree, and backed up at `~/Documents/GitHub/quant-pricing-lab-labs-backup-2026-09-13`.
- Delivered: Slices 0–17, i.e. Phases 0–4 of the working prompt. Per-slice
  records with measured numbers: `docs/CURRICULUM.md` (Delivered section),
  `.agents/brain/steering-brief.md` (newest slice on top), `docs/notes/*.md`
  (18 derivation notes), ADR-0004 (TDD process) and ADR-0005 (engine registry).
- Suite: 2585 tests; full `pytest -q` about 6 minutes locally, quick loop
  `pytest -q -m "not slow"` about 2 minutes (81 deselected). `ruff check .` clean.
- CI: green through the Slice 16 push. The Slice 17 push failed on one smoke
  key (see Open items, first entry).

## Decisions in force (from the Relentless interview, 2026-09-13)

1. Identity tie-breaker: numerical-methods lab with textbook provenance.
2. Recruiting does not reorder the curriculum.
3. Spine: multi-source method dependency graph (Fusai & Roncoroni Part II
   cases and Glasserman heavy; Shreve for derivations; Forsyth–Vetzal /
   Tavella–Randall for PDE; Hull as map; Gatheral/Heston/Lewis/Lord–Kahl/
   Carr–Madan for volatility and transforms).
4. Labs are private drivers, never public cargo; public cargo is `src/`,
   `tests/`, `qpl.cases`, and `docs/notes/`.
5. Public curriculum is the tested cases layer plus short derivation notes.
6. No human gating on derivations.
7. QuantLib-Python is the optional `[oracle]` extra, never the sole evidence.
8. Push only `dev/curriculum`; `main` is the owner's to fast-forward.
9. Market-data strand is frozen behind the `[data]` extra.

Provenance rules and evidence classes: ADR-0004 and `docs/CURRICULUM.md`.

## Open items (none require code to understand; all require a decision or a small change)

1. **CI red on the Slice 17 push.** `tests/test_examples_smoke.py` keys the
   `heston_calibration.py --case identifiability` output on
   `one_maturity_kappa_spread=2.50`. That spread is the width of the
   non-identifiable kappa–xi direction from six starts, so its digits are
   platform-dependent by construction (Linux lands at a different point on
   the flat direction). Remedy: key on the field name only
   (`"one_maturity_kappa_spread="`), as was done for the four earlier
   round-off keys. One line; not applied, per the owner's stop instruction.
2. **Suite runtime.** ~6 min full, ~2 min quick. Options published at the
   checkpoint: split CI (quick loop on push, full suite on `main` and
   nightly); cap the full suite by moving heavy studies to examples; accept.
3. **`main` and version.** Fast-forward `main` to `dev/curriculum` when
   reviewed; version is still 0.1.0 in `pyproject.toml` and `qpl.__version__`;
   a 0.3.0 tag was proposed after a consolidation slice.
4. **Presentation gaps.** `docs-site/` does not index `docs/notes/`, the
   cases layer, or `docs/CURRICULUM.md`; `CHANGELOG.md` records Slice 0 only;
   README quickstart predates most engines.
5. **Direction after Phase 4** (the checkpoint question left unanswered when
   the session was paused). Options published: consolidation slice then depth
   (recommended); Phase 5 breadth (multi-asset, rates); performance track;
   stop for review.

## Recommended next slices, if the campaign resumes

1. Consolidation: docs-site indexing of notes/cases/curriculum, CHANGELOG for
   Slices 0–17, version bump, README refresh, CI split. No new science.
2. QMC with Sobol and Brownian bridge (Fusai Ch 16; lifts two refused paths:
   stratified multi-step and bridge stratification).
3. Asian options by Laplace inversion and PDE (Fusai Ch 15; the
   `qpl.transforms` Stehfest toolkit still prices nothing).
4. Bates jumps through the Fourier framework (near-free given the
   characteristic-function interface).
5. Breeden–Litzenberger risk-neutral density and Dupire local vol
   (Fusai Ch 12, Gatheral).
6. Lookbacks (Fusai Ch 17); Andersen–Broadie duality bounds for LSM;
   penalty method versus PSOR.
7. Phase 5 (multi-asset on the existing copulas, rates on the existing CIR,
   performance) only when a case forces it.

## How to resume agent work

- Read `AGENTS.md`, `.agents/brain/brain.md`, this file, ADR-0004, ADR-0005,
  and `docs/CURRICULUM.md` (Phase plan and Delivered).
- Orchestration rules that worked: one worker subagent at a time; Opus for
  derivations, numerics design, stochastic volatility, calibration; Sonnet
  for well-specified implementation and docs; every slice stated as "a user
  can X by running Y, correct because Z"; the orchestrator re-runs an
  independent probe before accepting a slice; push only at slice boundaries.
- Worker rules learned the hard way: never pin a computed float with exact
  equality across platforms (`math.isclose(rel_tol=1e-12)`); never key
  example smoke output on machine-precision, cancellation-dominated, or
  non-identifiable digits; state the noise floor before fitting an order;
  when a slice statement's expectation fails, pin the measured truth as a
  NEGATIVE_FINDING rather than widening a band.
- Verification before declaring a slice done:
  `ruff check .`, `pytest -q` (or the quick loop during iteration),
  `QPL_LAB_SMOKE=1 MPLBACKEND=Agg pytest -q tests/test_labs_smoke.py`,
  `git status --short` empty.

## Where the campaign record lives

- Working prompt and interview: Relentless session
  `1aa44a71-b071-4979-9743-d6660a3b0ccf` (paused by the owner on 2026-09-14);
  prompt file `~/.local/share/relentless/sessions/<session>.prompt.md`.
- Orchestrator memory: `~/.claude/projects/-Users-ahmadalkadri-Documents-GitHub-quant-pricing-lab/memory/`.
- Per-slice measured tables: `docs/CURRICULUM.md` Delivered section and
  `docs/notes/`.
